#to-do
    #performance
    #change variables in application (step number, number of values, displayed char)
    #zoom-function?
    #structure of app -> window separate?
    # variatons like thicker dots for more dividers