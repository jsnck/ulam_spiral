#to-do
    #delete old spiral
    #performance
    #change step number via user input
    #zoom-function?
    #structure of app -> window separate?
    # variatons like thicker dots for more dividers
    # dynamic layout